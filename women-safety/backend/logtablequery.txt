CREATE TABLE GenderDistributionLog (
    id SERIAL PRIMARY KEY,
    timestamp TIMESTAMPTZ NOT NULL,
    location GEOMETRY(Point, 4326) NOT NULL,
    men_count INT CHECK (men_count >= 0),
    women_count INT CHECK (women_count >= 0)  
);